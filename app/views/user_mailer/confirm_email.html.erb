<!DOCTYPE html>
<html>
  <head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <% 
    host = @user.request_host || "http://localhost:3000"
    link = "#{host}/api/users/confirm?code=#{@user.confirmation_code}" 
    hours = Rails.configuration.new_user_confirmation_expires_in
  %>
  </head>
  <body>
    <p>
    Thanks for registering for the BBG Social Media Dashboard. 
    Please click or paste the link below in a Web browser to verify 
    your email.
    <br />
    <a href="<%= link %>"><%= link %></a>
    </p>
    <p>
    Please note that this confirmation link will expire in <%= hours %> hours.
    </p>
  </body>
</html>
